# 進度專案

## 使用規則

1. 每次開發結束後追加記錄，不覆蓋舊紀錄
2. 記錄要包含：日期、目標、變更、待處理
3. 只寫高價值資訊，不寫冗長過程

---

## 2026-04-15

### 本次目標

建立模板化的第一輪骨架，讓品牌與 webhook 設定開始集中化。

### 已完成

1. 建立單一設定入口 [js/config.js](C:/dev/陳情系統/leyicasedemo/js/config.js)
2. 新增 `SITE_TEMPLATE_CONFIG` 與 `SITE_UTILS.webhook()`
3. 前台首頁、表單、登入、政績頁、後台 shell 開始吃共用設定
4. 新增 [TEMPLATE_ARCHITECTURE.md](C:/dev/陳情系統/leyicasedemo/TEMPLATE_ARCHITECTURE.md)
5. 新增 [PROJECT_TODO.md](C:/dev/陳情系統/leyicasedemo/PROJECT_TODO.md)
6. 新增本輪協作主控文件與技能文件體系

### 待處理

1. 前台首頁導覽、分類 tab、流程區塊尚未完全設定驅動
2. 表單欄位與選項尚未完全設定驅動
3. `site_pages` 資料模型尚未建立
4. Notion schema 與 n8n workflow JSON 尚未正式產出
5. 政績專區尚未重構

### 下一步建議

1. 建立 `site_pages` schema
2. 實作後台頁面管理雛形
3. 產出第一批 n8n workflow JSON
4. 產出政績專區重構方案

---

## 2026-04-15

### 本次目標

完成「前台頁面可由後台管理」第一版雛形，先用本地 mock `site_pages` 跑通前後台流程。

### 已完成

1. 在 [js/config.js](C:/dev/陳情系統/leyicasedemo/js/config.js) 定義 `SITE_PAGE_SCHEMA_V1`、`SITE_PAGES_DEFAULT` 與 `SITE_PAGE_STORE`
2. 新增 [pages.html](C:/dev/陳情系統/leyicasedemo/pages.html) 作為後台頁面管理第一版雛形
3. 新增 [js/page-management.js](C:/dev/陳情系統/leyicasedemo/js/page-management.js)，提供頁面列表、排序、父子層、顯示開關與基本編輯
4. 新增 [js/site-navigation.js](C:/dev/陳情系統/leyicasedemo/js/site-navigation.js)，讓前台導覽改由 `site_pages` 本地設定生成
5. 更新 [admin.js](C:/dev/陳情系統/leyicasedemo/admin.js) 與 [admin.css](C:/dev/陳情系統/leyicasedemo/admin.css)，把頁面管理入口掛進後台側欄並補齊雛形樣式
6. 更新 [技能-前台頁面管理.md](C:/dev/陳情系統/leyicasedemo/技能-前台頁面管理.md) 補上 `site_pages` 第一版 schema 與實作策略

### 待處理

1. `site_pages` 目前仍是本地 `localStorage` mock，尚未串接 Notion / n8n
2. 前台目前只動態產生導覽，尚未做到 slug 對應完整動態頁內容
3. 後台尚未提供拖曳排序、批次調整與權限控管
4. 其他前台區塊如首頁流程、分類 tab、表單欄位仍未完全設定驅動

### 下一步建議

1. 把 `site_pages` 改成可由後台儲存到正式資料來源
2. 補上頁面內容設定欄位與 slug 對應渲染策略
3. 讓後台頁面管理支援拖曳排序與基本驗證規則
4. 逐步把首頁其他寫死區塊改成設定驅動

---

## 2026-04-15

### 本次目標

補強後台頁面管理雛形，先完成同層拖曳排序、刪除入口與基本資料驗證，不擴大到 Notion / n8n。

### 已完成

1. 更新 [js/page-management.js](C:/dev/陳情系統/leyicasedemo/js/page-management.js)，新增 `site_pages` 必填欄位、slug 唯一、父子層限制的驗證摘要
2. 在後台頁面列表加入同層拖曳排序，拖曳後會重新寫入同層 `sort_order`
3. 在頁面列表加入刪除入口；刪除父頁時會同步移除本地 mock 的子頁
4. 更新 [pages.html](C:/dev/陳情系統/leyicasedemo/pages.html) 與 [admin.css](C:/dev/陳情系統/leyicasedemo/admin.css)，補上驗證摘要區與拖曳狀態樣式
5. 更新 [技能-前台頁面管理.md](C:/dev/陳情系統/leyicasedemo/技能-前台頁面管理.md)，補充第一版排序與刪除限制

### 待處理

1. 拖曳排序目前只支援同一父層，跨父層搬移仍需用編輯表單調整 `parent_id`
2. 刪除與排序仍只寫入本地 `localStorage`，尚未接正式資料來源
3. 尚未加入復原機制、操作記錄或多人協作鎖定
4. 前台仍只根據 `site_pages` 產生導覽，尚未支援完整頁面內容動態渲染

### 下一步建議

1. 為 `site_pages` 補上匯出 / 匯入 JSON，方便複製到新客戶 demo
2. 規劃 Notion 版本的 `site_pages` database schema
3. 規劃 n8n 的 `pages` 讀寫 workflow
4. 再逐步補 slug 對應內容渲染策略

---

## 2026-04-15

### 本次目標

先完成 Notion DB 數量、命名與欄位規格，命名統一使用 `demoDB_` 前綴，後續再進入 n8n。

### 已完成

1. 新增 [NOTION_DATABASE_SCHEMA.md](C:/dev/陳情系統/leyicasedemo/NOTION_DATABASE_SCHEMA.md)
2. 定義第一版共 8 個 Notion DB：`demoDB_site_settings`、`demoDB_site_pages`、`demoDB_case`、`demoDB_inspection`、`demoDB_interpellation`、`demoDB_proposal`、`demoDB_achievement`、`demoDB_media_asset`
3. 明確列出各 DB 欄位、Notion 型別與用途
4. 更新 [技能-Notion資料庫.md](C:/dev/陳情系統/leyicasedemo/技能-Notion資料庫.md)，同步 `demoDB_` 命名與第一版建置原則

### 待處理

1. 尚未實際在 Notion 工作區建立 DB，需提供 Notion 父頁 URL 或 page ID
2. 尚未取得建立後的 Notion database IDs
3. n8n workflow 尚未開始，需等 Notion DB 建立完成後接續

### 下一步建議

1. 提供 Notion 父頁 URL 或 page ID
2. 先建立全部 8 個 DB，或先建立核心 4 個 DB：`demoDB_site_settings`、`demoDB_site_pages`、`demoDB_case`、`demoDB_media_asset`
3. Notion DB 建立後，再規劃 n8n 的 webhook 與 workflow JSON

---

## 2026-04-16

### 本次目標

建立 Notion DB 與 n8n 對應用文件，讓後續可補入每個 database ID。

### 已完成

1. 新增 [NOTION_DB_MAPPING.md](C:/dev/陳情系統/leyicasedemo/NOTION_DB_MAPPING.md)
2. 整理 8 個 Notion DB 的名稱、用途、欄位名稱、Notion 型別與 n8n 用途
3. 預留 `Notion Database ID` 與 `n8n 變數建議` 欄位
4. 明確記錄 n8n 讀取注意事項：案件查詢 key、公開過濾、展示排序與素材庫使用原則

### 待處理

1. 人工補上 8 個 Notion DB 的 database ID
2. 補完 ID 後，進入 n8n webhook 與 workflow JSON 規劃
3. 後續若 Notion 欄位改名，需同步更新 `NOTION_DB_MAPPING.md`

### 下一步建議

1. 先補 `demoDB_case`、`demoDB_site_pages`、`demoDB_site_settings` 的 ID
2. 再開始設計 n8n 的 `submit`、`query`、`list`、`pages` workflow
3. 最後補政績展示相關 workflow

---

## 2026-04-16

### 本次目標

補上 VS Code 檔案總管容易辨識的 `db_mapping.md`，避免只看到大寫檔名 `NOTION_DB_MAPPING.md`。

### 已完成

1. 新增 [db_mapping.md](C:/dev/陳情系統/leyicasedemo/db_mapping.md)
2. 在 `db_mapping.md` 預留 8 個 Notion DB 的 `Database ID`
3. 補上 n8n 第一批會優先使用的 DB 與 workflow 對應
4. 保留 [NOTION_DB_MAPPING.md](C:/dev/陳情系統/leyicasedemo/NOTION_DB_MAPPING.md) 作為完整欄位對照文件

### 待處理

1. 由使用者補入 8 個 Notion DB 的實際 ID
2. 補完 ID 後，開始建立 n8n workflow 規格

---

## 2026-04-16

### 本次目標

修正 Notion 對應文件，資料庫連線只要求填寫 Notion Database ID，不再要求 Data Source ID 或 Notion URL。

### 已完成

1. 更新 [NOTION_DB_MAPPING.md](C:/dev/陳情系統/leyicasedemo/NOTION_DB_MAPPING.md)，DB 對應總表只保留 `Notion Database ID` 與 n8n 變數名稱
2. 更新 [db_mapping.md](C:/dev/陳情系統/leyicasedemo/db_mapping.md)，同步簡化為只填 Database ID
3. 修正既有進度描述，移除 data source ID 需求，避免後續 n8n 對應混淆

### 待處理

1. 由使用者在 `NOTION_DB_MAPPING.md` 補入 8 個 Notion Database ID
2. 補完 ID 後開始規劃 n8n workflow

---

## 2026-04-16

### 本次目標

將使用者已補齊的 8 個 Notion Database ID 同步進專案設定，供後續 n8n workflow 使用。

### 已完成

1. 確認 [NOTION_DB_MAPPING.md](C:/dev/陳情系統/leyicasedemo/NOTION_DB_MAPPING.md) 已填入 8 個 Notion Database ID
2. 新增 [n8n/notion-database-ids.json](C:/dev/陳情系統/leyicasedemo/n8n/notion-database-ids.json)，提供 n8n 建立 workflow 時的 DB ID 對照
3. 更新 [.env.local.example](C:/dev/陳情系統/leyicasedemo/.env.local.example)，補入 8 個 `NOTION_DB_*` 環境變數

### 待處理

1. 尚未建立 n8n workflow，下一步可先做 `submit`、`query`、`list`
2. 前台目前仍透過現有 webhook / local mock 顯示，尚未直接讀 Notion
3. 若要看實際靜態介面，可先開 `index.html` 或 `pages.html`

---

## 2026-04-16

### 本次目標

清理重複 Notion 對應文件，只保留一份正式來源，避免 n8n 對應混淆。

### 已完成

1. 刪除重複的 `db_mapping.md`
2. 保留 [NOTION_DB_MAPPING.md](C:/dev/陳情系統/leyicasedemo/NOTION_DB_MAPPING.md) 作為唯一 Notion DB / n8n 對應文件
3. 確認後續只需在 `NOTION_DB_MAPPING.md` 補入 Notion Database ID

### 待處理

1. 由使用者在 `NOTION_DB_MAPPING.md` 補入 8 個 Notion Database ID
2. 補完 ID 後開始規劃 n8n workflow

---

## 2026-04-16

### 本次目標

在前台上方選單加入「政績牆」，並維持由 `site_pages` 頁面設定驅動導覽。

### 已完成

1. 更新 [js/config.js](C:/dev/陳情系統/leyicasedemo/js/config.js)，在 `SITE_PAGES_DEFAULT` 新增 `achievement-wall` 頁面設定
2. 「政績牆」以 `page_type = external` 指向 `leyi/achievements.html`
3. 更新 [js/site-navigation.js](C:/dev/陳情系統/leyicasedemo/js/site-navigation.js)，讓 `external` 頁面會渲染為可點擊連結
4. 更新 `SITE_PAGE_STORE.read()`，瀏覽器已有舊版 localStorage 時會自動併入新增的預設頁面
5. 更新 [技能-前台頁面管理.md](C:/dev/陳情系統/leyicasedemo/技能-前台頁面管理.md)，補充 `external` 類型第一版使用 `slug` 存 URL

### 待處理

1. 「政績牆」目前連到既有 `leyi/achievements.html`，尚未併入首頁 section 動態渲染
2. 後續若要由 Notion 管理外部連結，需確認 `demoDB_site_pages` 是否沿用 `slug` 存 URL 或新增 URL 欄位
3. n8n workflow 尚未建立，仍需接續設計 `submit`、`query`、`list`、`pages`

---

## 2026-04-16

### 本次目標

將政績牆由時間軸顯示改成項目卡片式展示，先以 6 筆展示資料與本地圖片檔名呈現。

### 已完成

1. 更新 [leyi/achievements.html](C:/dev/陳情系統/leyicasedemo/leyi/achievements.html)，移除前台時間軸、分類篩選、無限捲動與按讚流程
2. 改為政績項目卡片牆，只顯示政績名稱、政績時間、內容與圖片
3. 內建 6 筆展示用政績資料，暫不呈現類別
4. 新增 [leyi/assets/achievements/.gitkeep](C:/dev/陳情系統/leyicasedemo/leyi/assets/achievements/.gitkeep)，保留政績展示圖片資料夾

### 待處理

1. 使用者需放入 `leyi/assets/achievements/achievement-01.jpg` 到 `achievement-06.jpg`
2. 後續若要接 Notion，再將 6 筆本地 mock 替換為 `/achievements` webhook 回傳資料

---

## 2026-04-16

### 本次目標

統一 n8n webhook 命名原則與測試路徑，改為 `leyicasedemo/xxxx`。

### 已完成

1. 更新 [js/config.js](C:/dev/陳情系統/leyicasedemo/js/config.js)，`siteKey` 與 `webhookPrefix` 改為 `leyicasedemo`
2. 前端 webhook 產生規則改為 `https://drwu.zeabur.app/webhook-test/leyicasedemo/{endpoint}`
3. 更新 [.env.local.example](C:/dev/陳情系統/leyicasedemo/.env.local.example)，同步 `N8N_WEBHOOK_MODE=webhook-test` 與 `N8N_PREFIX=leyicasedemo`
4. 更新 [settings.html](C:/dev/陳情系統/leyicasedemo/settings.html) 與 [super-admin.html](C:/dev/陳情系統/leyicasedemo/super-admin.html)，後台顯示與測試路徑同步使用 `/webhook-test/leyicasedemo/...`
5. 更新既有 n8n 政績 workflow JSON，將 `leyidemo` 改為 `leyicasedemo`

### 待處理

1. n8n 正式啟用後若要改 production webhook，需將 `webhook-test` 改回 `webhook`
2. 尚未建立完整 `stats`、`list`、`query`、`submit` workflow JSON，只先統一路徑規則

---

## 2026-04-16

### 本次目標

將 webhook 路徑由測試路徑 `/webhook-test` 改為正式路徑 `/webhook`。

### 已完成

1. 更新 [js/config.js](C:/dev/陳情系統/leyicasedemo/js/config.js)，`webhookMode` 改為 `webhook`
2. 更新 [.env.local.example](C:/dev/陳情系統/leyicasedemo/.env.local.example)，`N8N_WEBHOOK_MODE=webhook`
3. 更新 [settings.html](C:/dev/陳情系統/leyicasedemo/settings.html) 與 [super-admin.html](C:/dev/陳情系統/leyicasedemo/super-admin.html)，後台產生路徑改為 `/webhook/leyicasedemo/...`

### 待處理

1. 尚未 push GitHub，待使用者確認後可直接上傳
2. n8n 中對應 workflow 需保持正式啟用狀態

---

## 2026-04-16

### 本次目標

調整首頁「立即陳情」與「查詢進度」CTA 在桌機與手機的比例，改善按鈕過大與不對稱問題。

### 已完成

1. 更新 [css/theme.css](C:/dev/陳情系統/leyicasedemo/css/theme.css)，將 Hero CTA 改為等寬雙欄 grid
2. 降低 CTA 高度、padding 與陰影強度，讓視覺更接近參考頁的柔和 pill button
3. 手機版維持兩顆按鈕並排，避免堆疊成過大的滿版按鈕

### 待處理

1. 尚未 push GitHub，待確認視覺方向後可上傳測試
2. 若後續要更貼近參考頁，可再調整 Hero 區塊留白、字級與統計卡片比例

---

## 2026-04-16

### 本次目標

測試 Codex 是否可透過 Notion MCP 連線並寫入 `demoDB_case`。

### 已完成

1. 使用 `NOTION_DB_MAPPING.md` 中的 `demoDB_case` Database ID 連線成功
2. 讀取到 Notion data source：`collection://343b3ad1-d1cd-813b-9f42-000ba9b185a2`
3. 成功建立測試案件：`MCP-20260416-0001`
4. 測試頁面 ID：`344b3ad1-d1cd-819c-90fe-f3ba62071cb3`
5. 測試資料設定為 `是否公開 = 否`、`資料狀態 = 測試資料`

### 待處理

1. `標籤` multi-select 不能用逗號字串寫入，後續 n8n / MCP 寫入時需改為正確 multi-select 格式或先略過
2. 可接續測試 n8n `/submit` 是否能建立同樣格式的 Notion case 資料
---

## 2026-04-19

### 本次進度

依照首頁主視覺結構回饋，調整 `hero-body` 內容順序，移除主標與副標，讓政績輪播 banner 成為第一屏主要視覺中心，按鈕置於輪播下方。

### 已完成

1. 從 `hero-body` 移除 `hero-title` 與 `hero-sub`。
2. 將 `hero-media-stage` 調整為 `hero-body` 的第一個主要內容。
3. 將 `hero-btns` 移到輪播 banner 下方。
4. 放大輪播 banner 尺寸並置中，桌機最大寬度調整為 1040px。
5. 調整手機與平板尺寸，避免 banner 與按鈕距離過大或重疊。

### 驗證

1. `node --check js/main.js` 通過。
2. `node --check js/config.js` 通過。
3. `git diff --check` 通過。
4. 確認 `index.html` 中已無 `hero-title` / `hero-sub`。

---

## 2026-04-19

### 本次進度

修正首頁政績輪播圖大量 404 問題。原因是輪播圖片 URL 放在 CSS 變數中，瀏覽器會以 `css/theme.css` 所在路徑解析，導致請求錯誤地變成 `/leyicasedemo/css/leyi/assets/...`。

### 已完成

1. 將首頁輪播圖片路徑由 `leyi/assets/...` 修正為 `../leyi/assets/...`。
2. 補上 favicon link，避免瀏覽器自動請求根目錄 `/favicon.ico` 造成 404。
3. 保留目前首頁版型與獨立輪播 banner，不調整其他 UI。

### 驗證

1. `node --check js/main.js` 通過。
2. `node --check js/config.js` 通過。
3. `git diff --check` 通過。

---

## 2026-04-19

### 本次進度

依照首頁主視覺回饋，修正上一版將政績圖做成整個背景造成的跑版問題；恢復原本首頁 hero 淺色主視覺架構，改為在 CTA 下方加入獨立的政績圖自動輪播 banner。

### 已完成

1. 將首頁 hero 背景恢復為原本淺色漸層與裝飾圓點設計。
2. 移除 hero 玻璃卡片包覆效果，恢復主標、副標、CTA 的原本版面層級。
3. 將 6 張政績圖改為獨立 banner 輪播區塊，不再覆蓋整個背景。
4. 保留自動輪播與淡入縮放效果。
5. 維持手動選單完全移除，不顯示分類按鈕。

### 驗證

1. `node --check js/main.js` 通過。
2. `node --check js/config.js` 通過。
3. `git diff --check` 通過。
4. 確認頁面無 `data-hero-slide` 與六個手動分類選單文字。

---

## 2026-04-19

### 本次進度

依照畫面回饋，移除首頁 hero 中不必要的背景切換選單，保留背景圖片自動輪播，避免第一屏出現過多分類按鈕干擾主要 CTA。

### 已完成

1. 移除首頁 `社區改善`、`道路安全`、`親子互動`、`樂齡關懷`、`校園守護`、`環境整理` 六個手動切換按鈕。
2. 移除對應的 `hero-media-controls` / `hero-media-dot` CSS。
3. 保留 6 張政績圖片作為 hero 背景自動輪播。
4. 保留首頁主 CTA：政績牆、立即陳情、查詢進度。

### 驗證

1. `node --check js/main.js` 通過。
2. `node --check js/config.js` 通過。
3. `git diff --check` 通過。

---

## 2026-04-19

### 本次進度

依照首頁 banner 新方向，移除原本紅框中的縮圖導覽列，改為以政績牆 6 張圖片作為首頁 hero 背景輪播，讓第一屏更偏向形象展示與政治人物主頁視覺。

### 已完成

1. 移除首頁 hero 下方 `hero-image-rail` 縮圖導覽列。
2. 使用 `leyi/assets/achievements/achievement-01.jpg` 到 `achievement-06.jpg` 作為 banner 背景輪播來源。
3. 新增背景淡入、縮放、亮度轉場效果，並支援自動輪播。
4. 新增手動切換按鈕，可直接切換不同政績形象背景。
5. 保留首頁三個 CTA：政績牆、立即陳情、查詢進度。
6. 保留泡泡互動與頁面 scroll progress bar。

### 驗證

1. `node --check js/main.js` 通過。
2. `node --check js/config.js` 通過。
3. `git diff --check` 通過。
4. 確認舊縮圖 class：`hero-image-rail`、`hero-shot`、`imageQuickIn` 已移除。

### 待處理事項

1. 若 GitHub Pages 首屏載入偏慢，後續應補一組壓縮後的 banner 專用 webp 圖片，不直接使用大尺寸展示圖。
2. 可再依第二版風格，將 hero 背景來源改由 `demoDB_site_settings` 或 `demoDB_achievements` 控制。

---

## 2026-04-16

### 本次進度

已透過 Notion MCP 實際連線並在 8 個 `demoDB_` 資料庫各建立 1 筆 MCP demo 假資料，供後續 n8n workflow 與前台介面測試使用。

### 已完成
1. `demoDB_site_settings`：建立 `MCP_DEMO_default`，含品牌、首頁文案、webhook base/prefix 與統計數字。
2. `demoDB_site_pages`：建立 `MCP Demo 政績牆`，用於測試 `external` 頁面、導覽名稱與 SEO 欄位。
3. `demoDB_case`：建立 `MCP-DEMO-CASE-0002`，用於測試案件查詢、公開狀態、案件來源與聯絡欄位。
4. `demoDB_inspection`：建立 `MCP Demo 公園步道會勘`，用於測試會勘列表與成果資料。
5. `demoDB_interpellation`：建立 `MCP Demo 校園通學安全質詢`，用於測試質詢資料呈現。
6. `demoDB_proposal`：建立 `MCP Demo 巷弄照明改善建議案`，用於測試提案資料呈現。
7. `demoDB_achievement`：建立 `MCP Demo 通學步道改善成果`，用於測試政績資料與圖片 URL 欄位。
8. `demoDB_media_asset`：建立 `MCP Demo 政績封面圖`，用於測試媒體素材資料。

### 待處理事項
1. 補齊尚未完成或尚未匯出到專案的 n8n workflow JSON。
2. 驗證 n8n 是否能依照 `NOTION_DB_MAPPING.md` 與實際 DB schema 正確讀寫 8 個資料庫。
3. multi-select 欄位本次暫未寫入，後續需確認 n8n / Notion MCP 對 `標籤` 欄位的可接受格式。
---

## 2026-04-16

### 本次進度

依照參考網站的 Hero CTA 視覺方向，重新調整首頁「立即陳情 / 查詢進度」按鈕與手機版排版。

### 已完成
1. 調整桌機版 CTA：加大按鈕寬度、高度、字級與陰影，維持置中並排。
2. 調整手機版 CTA：由兩欄並排改為上下排列，避免偏移、擁擠與比例錯誤。
3. 放大手機版 Hero 標題、副標與標籤文字，強化視覺層級。
4. 增加 CTA 區塊與統計卡片之間的留白，讓手機版更接近參考網站的大按鈕節奏。

### 待處理事項
1. 需要上 GitHub Pages 後以實機手機檢查最終比例。
2. 若仍覺得 Hero 區過高，可再微調手機版 `hero-unit` padding 與 CTA margin。
---

## 2026-04-16

### 本次進度

再次修正首頁 Hero CTA 的 UI 與 RWD。前一版重疊主因是按鈕 `width: 100%` 搭配 padding 後未使用 `box-sizing: border-box`，實際寬度超出欄位。

### 已完成
1. 將 Hero CTA 由 grid 改為 flex，桌機版使用固定按鈕寬度與可控間距，避免兩顆按鈕重疊。
2. 新增 Hero CTA 局部 `box-sizing: border-box`，修正 padding 撐爆寬度的問題。
3. 手機版按鈕改為上下排列，尺寸調整為較平衡的高度與字級。
4. 降低 CTA 字重與陰影強度，避免按鈕與文字比例過重。

### 待處理事項
1. 推送後需以 GitHub Pages 手機實機檢查 CTA 是否已無重疊。
2. 若仍希望更接近參考網站，可下一步重寫整個 Hero section，而不只修 CTA。
---

## 2026-04-16

### 本次進度

完成 Demo logo 套用與 Super Admin 靜態 demo 登入。

### 已完成
1. 偵測並沿用已上傳的 logo：`leyi/assets/brand/logo.png`。
2. 前台首頁導覽 logo 改為圖片顯示。
3. 政績牆頁面導覽 logo 改為圖片顯示。
4. 登入頁品牌 logo 改為圖片顯示。
5. 後台 sidebar logo 改為圖片顯示，並處理根目錄與 `admin/` 子目錄的相對路徑。
6. 新增靜態 demo Super Admin 登入：`superadmin@demo.local` / `demo2026`。
7. Super Admin 頁面新增無後端 API 時的 demo fallback 資料，方便 GitHub Pages 直接查看 UI。

### 待處理事項
1. 後續若接正式後端，需移除或關閉靜態 demo 登入。
2. 第二套風格可接 `demoDB_site_settings` + n8n `/settings` workflow 後再動態切換。
---

## 2026-04-16

### 本次進度

修正 Super Admin demo 登入體驗，避免 GitHub Pages 靜態環境誤走後端 `/api/auth/login` 造成連線失敗。

### 已完成
1. 在登入頁新增 `Demo Super Admin 直接進入` 按鈕。
2. demo 帳密判斷改為忽略 Email 大小寫與密碼前後空白。
3. 若使用 `@demo.local` 但密碼錯誤，直接提示 demo 密碼，不再呼叫後端 API。

### 待處理事項
1. 正式後端完成後，需移除或關閉 demo 直入按鈕。
---

## 2026-04-16

### 本次進度

修正後台頁面 CSS 未套用問題。原因是根目錄後台頁引用 `css/admin.css`，但實際樣式檔位於根目錄 `admin.css`。

### 已完成
1. 將根目錄後台頁 stylesheet 路徑由 `css/admin.css` 改為 `admin.css`。
2. 修正範圍包含 dashboard、cases、settings、users、super-admin、pages、政績資料管理相關頁。
3. `admin/achievements.html` 位於子目錄，已維持使用 `../admin.css`。

### 待處理事項
1. 推送後需重新整理 GitHub Pages 後台頁面檢查樣式是否正常載入。
---

## 2026-04-16

### 本次進度

依照里長服務情境重構政績牆內容呈現，補上標籤、代表性服務事項與政績詳情彈窗。

### 已完成
1. 政績牆 banner 下方文案改為里長角度，強調里民需求、現場協調與完成改善。
2. 新增代表性服務事項關鍵字：帶動社區、親子互動、樂齡互動、道路安全、校園守護、環境改善、鄰里照顧。
3. 每張政績卡片新增多個標籤，例如教育、學校、道路安全、公園、親子互動、樂齡互動等。
4. 每張卡片新增「查看完整過程」按鈕，點擊後以彈窗顯示完整詳情。
5. 詳情彈窗包含完成時間、標籤、摘要、服務過程、現場圖片集與影片紀錄預留區。

### 待處理事項
1. 後續接 Notion `demoDB_achievement` 時，需把 tags、process、images、videoUrl 對應進資料欄位或 n8n 回傳格式。
2. 若要正式展示影片，需補上 YouTube / Cloudinary / Notion file URL 欄位。
---

## 2026-04-16

### 本次進度

依照政績牆新版資訊架構，將頁面整理為「精選成果、成果牆、成果來源」三段式呈現，並移除原本的「代表性服務事項」標籤文字。

### 已完成
1. 新增精選成果區塊，優先呈現最有感的 3 件服務成果。
2. 保留成果牆卡片列表，讓所有政績仍可集中瀏覽。
3. 新增成果來源區塊，說明資料可來自陳情案件、現場會勘、社區活動與機關協調。
4. 政績資料結構新增 `featured` 與 `source` 欄位，方便後續銜接 Notion `demoDB_achievement`。
5. 詳情彈窗與卡片同步顯示成果來源資訊。

### 待處理事項
1. 後續接 n8n `/achievements` 時，需讓回傳資料包含 `featured`、`source`、`tags`、`process`、`images`、`videoUrl`。
2. 若要第二套風格，建議由 `demoDB_site_settings` 控制 theme key，再切換 CSS 變數與版型模組。
---

## 2026-04-16

### 本次進度

完成首頁 Hero 標籤移除，並讓政績牆具備第一版 Notion / n8n 串接能力。

### 已完成
1. 移除首頁 Hero 上方「數位透明・為民服務」膠囊標籤。
2. 政績牆前台改為優先讀取 `SITE_UTILS.webhook('achievements')`，正式路徑為 `/webhook/leyicasedemo/achievements`。
3. n8n workflow `WF-11_achievements_list.json` 已改成使用 `demoDB_achievement` 實際 Database ID。
4. n8n 欄位 mapping 已改為目前文件欄位：`政績標題`、`政績類型`、`議題類型`、`標籤`、`發布日期`、`摘要`、`詳細內容`、`封面圖片`、`成果前圖片`、`成果後圖片`、`是否公開`、`是否精選`、`顯示排序`、`資料狀態`、`來源連結`。
5. 前台保留 demo fallback：n8n 尚未啟用或 API 失敗時，仍會顯示本地展示資料。
6. 已測試正式 webhook，目前回傳 404，表示 n8n 尚未匯入或尚未啟用 production workflow。

### 待處理事項
1. 需在 n8n 匯入並啟用 `n8n/WF-11_achievements_list.json`。
2. n8n 啟用後，再測試 `https://drwu.zeabur.app/webhook/leyicasedemo/achievements` 是否回傳 `success: true` 與 `items`。
3. 若政績詳情要有更完整服務過程，後續可在 Notion 增加 `服務過程` 欄位，或先用 `詳細內容` 分段呈現。
---

## 2026-04-16

### 本次進度

修正政績牆手機版卡片高度異常，並依需求移除成果來源區塊。

### 已完成
1. 移除政績頁「成果來源」整段 HTML 與相關 CSS。
2. 修正成果牆卡片 `min-height: 100%` 造成手機單欄卡片可能被撐高的問題，改為自然高度。
3. 成果牆 grid 改為 `align-items: start`，避免卡片跟隨同列最高項目被拉長。
4. 卡片按鈕取消 `margin-top: auto`，避免 flex 內容被拉伸時按鈕下方出現大面積留白。
5. 卡片摘要加入行數限制，手機版最多 5 行，避免 Notion / n8n 長內容直接撐爆卡片。
6. API 正規化時新增文字清理，移除多餘換行與連續空白。

### 待處理事項
1. 推送後需用手機實機重新整理 GitHub Pages，確認成果牆卡片已自然收合。
2. 若仍有極端長文字，下一步可把卡片摘要固定讀 Notion `摘要`，詳情彈窗再讀 `詳細內容`。
---

## 2026-04-16

### 本次進度

補上案件處理儀表板需要的 n8n workflow 匯入檔。

### 已完成
1. 新增 `n8n/WF-04_dashboard.json`。
2. webhook 路徑設定為 `GET /webhook/leyicasedemo/dashboard`。
3. workflow 直接查詢 `demoDB_case`，Database ID 使用 `343b3ad1d1cd800391f1ff4907635829`。
4. Code node 會依 `period=7d|30d|90d` 計算前台需要的 `chart`、`monthStats`、`typeCounts`、`areaCounts`、`statusCounts`。
5. 已檢查 workflow JSON 可解析，Code node JavaScript 語法通過。

### 待處理事項
1. 需將 `n8n/WF-04_dashboard.json` 匯入 n8n 並啟用 workflow。
2. 啟用後測試 `https://drwu.zeabur.app/webhook/leyicasedemo/dashboard?period=7d`。
3. 若 Notion 節點匯入後欄位條件不相容，優先檢查 `是否公開`、`建立時間`、`案件類型`、`案件狀態`、`結案日期` 欄位名稱是否一致。
---

## 2026-04-16

### 本次進度

新增 30 筆 demoDB_case 擬真案件資料，提供前台列表、查詢、統計與儀表板測試使用。

### 已完成
1. 已在 Notion `demoDB_case` 新增案件編號 `SVC-20260416-3001` 至 `SVC-20260416-3030`。
2. 每筆皆包含案件標題、案件類型、案件區域、案件狀態、公開摘要、回覆內容、案件原始內容、公開詳情、是否公開、案件來源、緊急程度、最後更新說明、查詢關鍵字、資料狀態、聯絡資料與顯示排序。
3. 案件內容涵蓋交通運輸、公共設施、環境建管、警消政風、其他服務等類型。
4. 狀態包含已結案與處理中，已結案案件補上結案日期，方便 dashboard 統計測試。
5. Notion 建立工具已回傳 30 筆 page ID；另以 Notion 搜尋抽查可查到本批案件。

### 待處理事項
1. 若 n8n list / dashboard workflow 已啟用，需重新測試前台是否讀到新增資料。
2. 若要做更完整查詢測試，可挑選 `SVC-20260416-3001`、`SVC-20260416-3024`、`SVC-20260416-3030` 進行單筆查詢驗證。

---

## 2026-04-16

### 本次進度

檢查案件處理儀錶板的案件趨勢顯示問題，確認正式 `/webhook/leyicasedemo/dashboard?period=7d` 可正常回傳 200，但因 demo 案件是批次建立，Notion `建立時間` 幾乎集中在 04/15、04/16，導致趨勢圖視覺上失真。

### 已完成

1. 更新 `n8n/WF-04_dashboard.json`，案件趨勢改用本地日期 key，避免 `toISOString()` 造成時區日期偏移。
2. 新增 demo 批次資料偵測：若大量案件同日建立，workflow 會用結案日期與排序值把趨勢分散到期間內，讓 Demo 儀錶板較接近真實趨勢。
3. 回傳資料新增 `trendMode`，可辨識目前為 `created_time` 或 `demo_spread`。
4. 更新 `js/main.js`，dashboard webhook 若失敗或 `success=false`，前端會顯示明確錯誤，不再靜默失敗。
5. 已完成 `js/main.js`、`n8n/WF-04_dashboard.json` 與 workflow code node 語法檢查。

### 待處理

1. 需要將新版 `n8n/WF-04_dashboard.json` 匯入或同步到 n8n 正式 workflow，GitHub 更新不會自動改動已啟用的 n8n workflow。
2. 若未來要讓趨勢完全真實，建議在 `demoDB_case` 補一個可手動控制的公開日期或趨勢日期欄位，不只依賴 Notion 系統 `建立時間`。
---

## 2026-04-16

### 本次進度

修正案件處理儀錶板的案件趨勢空白問題。正式 dashboard webhook 已確認有回傳 `chart` 資料，問題集中在前端長條圖 HTML 屬性字串斷裂與 CSS 高度呈現。

### 已完成

1. 修正 `js/main.js` 的 `renderStackedBar()`，移除可能破壞 HTML 結構的亂碼 title 字串，改為穩定的長條圖 DOM。
2. 更新 `css/theme.css`，補上長條圖軌道最小高度、長條明確寬度、分段寬度與底部對齊。
3. 已確認 `https://drwu.zeabur.app/webhook/leyicasedemo/dashboard?period=7d` 回傳 `trendMode: demo_spread` 與 7 日 chart 資料。

### 待處理

1. 推送後需等待 GitHub Pages 快取更新，再重新整理前台頁面測試案件趨勢。
2. 若仍空白，下一步直接在瀏覽器 Elements 檢查 `.bar-stacked` 是否有正確 `height` 與子層 segment。
---

## 2026-04-16

### 本次進度

依需求移除首頁案件處理儀錶板中的「案件趨勢圖表」，改為更適合 Demo 與民眾視角的「近期公開回覆」區塊。

### 已完成

1. 更新 `index.html`，將原本案件趨勢卡片替換為近期公開回覆卡片，保留「當月案件處理量」與「案件類別比例」。
2. 更新 `js/main.js`，新增 `loadRecentReplies()` 與 `renderRecentReplies()`，直接使用既有 `/webhook/leyicasedemo/list?page=1` 公開案件資料，不新增 n8n workflow。
3. 更新 `css/theme.css`，新增近期公開回覆卡片、標籤、狀態、案件摘要與底部資訊樣式。
4. 已確認 `/webhook/leyicasedemo/list?page=1` 正常回傳公開案件與回覆內容。
5. 已通過 `node --check js/main.js`。

### 待處理

1. 推送後等待 GitHub Pages 更新，再確認首頁儀錶板區塊視覺效果。
2. 若後續需要點擊近期公開回覆開啟詳情，可再補案件詳情 modal 或連動案件查詢。
---

## 2026-04-17

### 本次進度

修正首頁「如何提出陳情？」流程區塊在手機版的步驟卡片跑版問題，並統一流程卡片視覺。

### 已完成

1. 更新 `css/theme.css` 的 `.flow-step`，加入 `box-sizing: border-box`，避免手機版 `width: 100%` 加上 padding 後超出容器。
2. 將流程卡片改為穩定的 flex column 卡片版式，統一桌機與手機的視覺語言。
3. 將 `.flow-num` 從原本小型浮動標籤改為大型數字章牌，手機版數字再放大。
4. 手機版流程改為單欄、最大寬度 420px、置中排列，避免每個步驟寬度與位置不一致。

### 待處理

1. 推送後等待 GitHub Pages 更新，再以手機尺寸確認「如何提出陳情？」區塊是否已正常。
---

## 2026-04-17

### 本次進度

重整 `PROJECT_TODO.md`，把早期規劃、重複項目與已完成事項整理成目前可執行的待辦清單。

### 已完成

1. 將 TODO 改為「已完成、進行中、下一步建議、暫緩」結構。
2. 補上目前已完成的前台、後台、Notion、n8n 與政績牆進度。
3. 明確列出下一步優先順序：n8n workflow 對照、site_settings、site_pages、交付 SOP。
4. 保留 Notion DB 與 n8n webhook 清單，方便後續對照。
5. 明確標示任務派工、任意深度頁面樹、block builder、正式 RBAC 等項目暫緩。

### 待處理

1. 下一步建議先整理 n8n workflow 實際狀態，確認 GitHub JSON 與 n8n 線上版本一致。

---

## 2026-04-17

### 本次進度

完成 n8n workflow 第一輪實際狀態盤點，並新增專案對照文件。

### 已完成

1. 新增 `N8N_WORKFLOW_STATUS.md`，整理正式 webhook、repo JSON、資料來源與下一步缺口。
2. 已確認正式 webhook 可用：`/stats`、`/list`、`/query`、`/dashboard`、`/achievements`。
3. 已確認正式 webhook 尚未存在或未啟用：`/inspections`、`/interpellations`、`/proposals`、`/pages`、`/settings`。
4. 標記 `/submit` 與 `/achievements/like` 為 POST workflow，需用安全測試資料或 n8n UI 再確認。
5. 更新 `PROJECT_TODO.md`，把 n8n 盤點結果與待補 JSON 匯出工作寫入下一步。

### 待處理

1. 匯出線上可用的 `stats`、`list`、`query`、`submit` workflow JSON 到 `n8n/`。
2. 優先建立 `/settings` workflow，讓第二套風格與品牌設定可由 `demoDB_site_settings` 控制。
3. 接著建立 `/pages` workflow，讓前台導覽正式讀取 `demoDB_site_pages`。

---

## 2026-04-17

### 本次進度

完成 `site_settings` 第一版前端設定中介層與 n8n workflow 草案，先讓前台與後台具備可運作 fallback，不等待 n8n 正式啟用。

### 已完成

1. 更新 `js/config.js`，新增 `SITE_SETTINGS_STORE` 與 `SITE_SETTINGS_SCHEMA_V1`。
2. 前台會先嘗試讀取 `/webhook/leyicasedemo/settings`，失敗時改用 localStorage 或預設設定，避免 GitHub Pages 空白。
3. 更新 `js/main.js`，在首頁初始化前套用 site settings，並重新組裝 webhook endpoint。
4. 更新 `admin.js`，讓 GitHub Pages 無後端時的 `/settings` GET/PATCH 可落到 localStorage fallback。
5. 更新 `settings.html`，新增品牌名稱、品牌簡稱、後台名稱、Logo URL、主色、輔助色、首頁標題與首頁副標欄位。
6. 新增 `n8n/WF-13_settings.json`，對應 `demoDB_site_settings` 並回傳前台需要的 settings JSON。
7. 更新 `N8N_WORKFLOW_STATUS.md` 與 `PROJECT_TODO.md`。

### 驗證

1. `node --check js/config.js` 通過。
2. `node --check js/main.js` 通過。
3. `n8n/WF-13_settings.json` 可正常 JSON parse。

### 待處理

1. 將 `n8n/WF-13_settings.json` 匯入 n8n 並啟用正式 `/settings`。
2. 確認 Notion `demoDB_site_settings` 有一筆 `是否啟用 = true` 的設定資料。
3. 後台設定頁目前在 GitHub Pages 仍以 localStorage fallback 儲存，正式寫回 Notion 需另建 PATCH/POST workflow。
4. 下一步接續 `site_pages` 的 `/pages` workflow 與前台導覽資料化。

---

## 2026-04-17

### 本次進度

準備 `site_pages` 的 n8n `/pages` workflow 與 Notion 預設資料交付內容，讓使用者可先手動建立資料與匯入 workflow。

### 已完成

1. 新增 `n8n/WF-14_pages.json`，對應 `demoDB_site_pages` 並輸出 `pages` 與最多 2 層的 `tree`。
2. 更新 `N8N_WORKFLOW_STATUS.md`，將 `/pages` 標記為已有 repo JSON、待匯入 n8n。
3. 更新 `PROJECT_TODO.md`，把 `site_pages` 待辦改為匯入 workflow、建立預設頁面資料與前台導覽正式資料化。

### 待處理

1. 使用者需先在 `demoDB_site_pages` 建立預設頁面資料。
2. 使用者需匯入並啟用 `n8n/WF-14_pages.json`。
3. 下一步開發前台導覽讀取 `/pages`，失敗時 fallback 到 `SITE_PAGE_STORE`。

---

## 2026-04-19

### 本次進度

依 Demo 首屏展示需求調整首頁 banner、CTA、品牌名稱、透明 Logo 呈現與政績牆頁 Header 一致性。

### 已完成

1. 首頁 hero 主標改為「數位時代 / 市民新標準」。
2. 首頁 hero 副標改為「放心交給我」。
3. 首頁 hero CTA 改為三顆按鈕：`政績牆`、`立即陳情`、`查詢進度`。
4. 品牌名稱預設改為「樂藝數位服務處」。
5. Logo 改為透明 PNG 原圖呈現，不再疊加漸層底色，圖片使用 `object-fit: contain`。
6. 政績牆頁 Header 改為與首頁一致的品牌、Logo、導覽語彙與 CTA。
7. 政績牆頁初始化時會讀取 `/settings` 並重新組裝 `achievements` webhook。
8. 首頁增加低風險動態：標題、副標、按鈕依序浮現，背景幾何緩慢漂移，並支援 `prefers-reduced-motion`。

### 驗證

1. `node --check js/config.js` 通過。
2. `node --check js/main.js` 通過。

### 待處理

1. 因首頁會讀取 `demoDB_site_settings`，若 Notion 仍是舊品牌或舊文案，需同步更新該設定列。
2. 後續可把首頁動態再升級為數字滾動、CTA 光暈掃描或政績卡片預覽，但先避免過度動畫影響手機效能。

---

## 2026-04-19

### 本次進度

強化首頁 Demo 第一眼視覺與互動效果，讓潛在客戶更快理解政績牆、陳情與查詢流程。

### 已完成

1. 首頁三顆 CTA 改為一致的漸層按鈕樣式，移除按鈕 emoji，提升字重與字距。
2. 新增頁面頂部 scroll progress bar，顯示目前瀏覽進度。
3. 首頁 hero 新增泡泡浮動效果，並支援滑鼠 / 觸控 pointer 輕微互動。
4. 首頁 banner 加入 6 張政績牆圖片快速導覽特效，圖片來源為 `leyi/assets/achievements/achievement-01.jpg` 至 `achievement-06.jpg`。
5. 圖片導覽卡片支援 staggered quick-in 動畫與 hover 放大。
6. 手機版圖片導覽改為 3 欄 / 2 欄 RWD，避免破版。

### 驗證

1. `node --check js/main.js` 通過。
2. `node --check js/config.js` 通過。

### 待處理

1. 推送後等待 GitHub Pages 更新，確認手機版 hero 圖片導覽高度與按鈕比例。
2. 若圖片載入過慢，下一步應建立壓縮縮圖版本，避免首頁首屏流量過大。
